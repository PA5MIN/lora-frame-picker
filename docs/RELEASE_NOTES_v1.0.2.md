# v1.0.2

## 中文

- 修复 Windows 隐私测试在清空环境变量后调用 `Path.home()` 导致的 CI 失败。
- 应用功能和实际保存路径不变。

## English

- Fixed a Windows privacy test that called `Path.home()` after clearing the environment used to resolve the home directory.
- Application behavior and real output locations are unchanged.
